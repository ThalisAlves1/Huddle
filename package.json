{
  "name": "huddle-presenca-workspace",
  "private": true,
  "workspaces": [
    "Presen-aHusf-main"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=huddle-presenca",
    "build": "npm run build --workspace=huddle-presenca",
    "lint": "npm run lint --workspace=huddle-presenca",
    "preview": "npm run preview --workspace=huddle-presenca"
  }
}
